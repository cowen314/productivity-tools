import argparse
import subprocess
import pathlib

# set up command line interface

# parse variables

# clone some template repo into current repo (use subprocess)
# remove the git dir
# do.... other stuff??
# clone the gitattributes template from the gitattributes template repo
# hit the command to add a new repo to the remote
# be sure to handle failures here
# push up to the remote
# 

if __name__=="__main__":
    pass